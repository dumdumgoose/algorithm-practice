# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Idea files, which we don't really need to be in this repo
.idea

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
